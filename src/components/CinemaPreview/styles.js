import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  view: {
    paddingBottom: 10,
    paddingLeft: 5,
  },
  name: {
    fontSize: 48,
  },
  website: {
    fontSize: 24,
    fontWeight: 'bold',
  },
});
